<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.donotforget.user.donotforget.ShowRemindersActivity"
    android:orientation="vertical"
    android:background="#1DA1F2"
    android:paddingStart="@dimen/activity_horizontal_margin"
    android:textDirection="locale"
    android:layoutDirection="locale"
    android:paddingEnd="@dimen/activity_horizontal_margin">
    <LinearLayout
        android:orientation="horizontal"
        android:layout_width="match_parent"
        android:textDirection="locale"
        android:layoutDirection="locale"
        android:layout_height="wrap_content">

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:id="@+id/txtMessageReminders"
            android:hint="@string/deleteReminder"
            android:maxLength="100"
            android:maxLines="2"
            android:textDirection="locale"
            android:layoutDirection="locale"
            android:minHeight="50dp" />
    </LinearLayout>
    <LinearLayout
        android:orientation="horizontal"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textDirection="locale"
        android:layoutDirection="locale"
        android:gravity="center_horizontal">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/reminders_to"
            android:id="@+id/tvRemindersTo"
            android:textColor="#FFFFFF"
            android:textSize="16sp"
            android:textDirection="locale"
            android:layoutDirection="locale"
            android:textStyle="bold" />

        <Spinner
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:id="@+id/spinReminders"
            android:layout_marginLeft="5dp"
            android:textDirection="locale"
            android:layoutDirection="locale"
            android:layout_marginStart="5dp" />
    </LinearLayout>

    <FrameLayout
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:id="@+id/reminders_container"
        android:layout_weight="0.1"
        android:textDirection="locale"
        android:layoutDirection="locale"
        android:layout_marginTop="20dp"></FrameLayout>

</LinearLayout>
